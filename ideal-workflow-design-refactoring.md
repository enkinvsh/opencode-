# 🎯 Идеальный Workflow для дизайн-рефакторинга

## Для дизайн-рефакторинга: **@plan → /start-work** (точность и контроль)

Дизайн-рефакторинг — это **сложная многошаговая задача**, где критичны:
- Понимание текущей архитектуры
- План миграции без поломок
- Верификация каждого шага

**❌ НЕ используй `ulw`** для рефакторинга — слишком высок риск непредсказуемых изменений.

---

## 📋 Пошаговый Workflow

### **Этап 1: Инициация планирования**

```
@plan Хочу сделать дизайн-рефакторинг UI компонента Button:
- Сейчас использует inline styles
- Нужно перевести на CSS modules
- Добавить темизацию (light/dark)
- Сохранить все существующие пропсы
```

**Что произойдет:**
1. Запустится **Prometheus** (планировщик)
2. Он перейдет в режим интервью и начнет задавать вопросы:
   - "Сколько компонентов использует Button?"
   - "Есть ли unit тесты?"
   - "Нужно ли сохранять обратную совместимость?"
   - "Какие браузеры должны поддерживаться?"

### **Этап 2: Интервью (самая важная часть!)**

**Отвечай на вопросы Prometheus максимально подробно:**

```
Пример диалога:

Prometheus: "Сколько компонентов в проекте используют Button?"
Ты: "Около 40 компонентов в /src/components и /src/pages"

Prometheus: "Есть ли автоматические тесты для Button?"
Ты: "Да, Jest unit тесты в Button.test.tsx"

Prometheus: "Какая приоритетность: скорость миграции или zero downtime?"
Ты: "Zero downtime — это production код"
```

**Prometheus будет:**
- ✅ Исследовать кодовую базу (через explore агента)
- ✅ Фиксировать детали в `.sisyphus/drafts/button-refactor.md`
- ✅ Уточнять риски и краевые случаи

### **Этап 3: Консультация и финализация**

Когда Prometheus соберет достаточно информации, он:

1. **Консультируется с Metis** (находит скрытые проблемы)
2. **Предлагает черновик плана:**

```markdown
## Work Plan: Button Design Refactor

### Phase 1: Preparation
- [ ] Audit all Button usages (grep, AST analysis)
- [ ] Snapshot current visual state (screenshot tests)
- [ ] Create CSS modules structure

### Phase 2: Migration
- [ ] Implement new Button with CSS modules
- [ ] Add theme provider
- [ ] Create compatibility layer (supports both old/new)

### Phase 3: Rollout
- [ ] Migrate components in /src/components (20 files)
- [ ] Migrate pages in /src/pages (20 files)
- [ ] Update tests

### Phase 4: Verification
- [ ] Run full test suite
- [ ] Visual regression tests
- [ ] Remove old inline styles

### Rollback Strategy
- Keep old Button as ButtonLegacy for 2 sprints
```

3. **Ты говоришь:** `"Make it a plan"`
4. **Prometheus сохраняет план в:** `.sisyphus/plans/button-refactor.md`

### **Этап 4: Исполнение**

```
/start-work
```

**Что произойдет:**

1. **Atlas** (исполнитель) загружает план из `.sisyphus/plans/button-refactor.md`
2. Создает **boulder.json** для отслеживания прогресса:
   ```json
   {
     "plan": "button-refactor",
     "currentPhase": 1,
     "completedTasks": [],
     "sessionId": "ses_abc123"
   }
   ```

3. **Atlas начинает делегировать:**
   ```
   Phase 1, Task 1: "Audit Button usages"
   → delegate_task(subagent="explore", background=true)
   
   Phase 2, Task 1: "Implement new Button"
   → delegate_task(
       category="visual-engineering",
       load_skills=["frontend-ui-ux"],
       prompt="..."
     )
   
   Phase 3: "Migrate components"
   → delegate_task(category="quick", load_skills=[])
   ```

4. **После каждой задачи:**
   - ✅ Запускает тесты
   - ✅ Проверяет через `lsp_diagnostics`
   - ✅ Обновляет `boulder.json`
   - ✅ Продолжает дальше

### **Этап 5: Непрерывность**

Если сессия прервется (закрыл терминал, отключился интернет):

```
/start-work
```

**Atlas автоматически:**
1. Найдет `boulder.json`
2. Прочитает: "Был на Phase 2, Task 3"
3. Продолжит с того же места с тем же `sessionId`

---

## 🔄 Альтернативный Workflow (для простых случаев)

### Если рефакторинг **локальный и простой**:

```
ulw Отрефактори компонент Button.tsx:
- Замени inline styles на CSS modules
- Файл: /src/components/Button.tsx
- Сохрани все существующие пропсы
- Запусти тесты после
```

**Когда это ОК:**
- ✅ Один файл
- ✅ Нет сложных зависимостей
- ✅ Есть тесты для верификации

**Когда НЕ ОК:**
- ❌ Затрагивает 10+ файлов
- ❌ Требует миграции данных
- ❌ Сложная логика rollback

---

## 🎓 Практические Советы

### **1. Не спеши с "Make it a plan"**

Пример **плохого** подхода:
```
Ты: @plan Отрефактори UI
Prometheus: "Уточни что именно?"
Ты: "Ну всё"
Prometheus: [создает расплывчатый план]
Ты: "Make it a plan" ← СЛИШКОМ РАНО
```

Пример **хорошего** подхода:
```
Ты: @plan Отрефактори UI
Prometheus: [задает 5-7 вопросов]
Ты: [отвечаешь подробно]
Prometheus: "Вот черновик плана. Что-то упустил?"
Ты: "Добавь миграцию для старых пропсов"
Prometheus: [дополняет]
Ты: "Отлично, make it a plan" ← ПРАВИЛЬНО
```

### **2. Используй файлы в drafts/**

Prometheus сохраняет все обсуждение в `.sisyphus/drafts/`. Если нужно прерваться:

```
Ты: "Сохрани что мы обсудили, продолжим завтра"
Prometheus: [сохраняет в drafts/button-refactor.md]

[На следующий день:]
Ты: @plan Продолжим рефакторинг Button
Prometheus: [читает drafts/, продолжает с того места]
```

### **3. Один план = один файл**

**❌ НЕ делай:**
```
.sisyphus/plans/
  ├── button-refactor-phase1.md
  ├── button-refactor-phase2.md
  └── button-refactor-phase3.md
```

**✅ Делай:**
```
.sisyphus/plans/
  └── button-refactor.md  (все фазы внутри)
```

Это исключает фрагментацию контекста.

---

## 📊 Сравнение подходов

| Сценарий | Workflow | Время | Точность | Контроль |
|----------|----------|-------|----------|----------|
| **Простой рефакторинг 1 файла** | `ulw` | 🟢 Быстро | 🟡 Средняя | 🟡 Средний |
| **Рефакторинг 5-10 файлов** | `@plan` → `/start-work` | 🟡 Средне | 🟢 Высокая | 🟢 Полный |
| **Масштабный редизайн (50+ файлов)** | `@plan` → `/start-work` | 🔴 Долго | 🟢 Максимальная | 🟢 Полный |
| **"Хочу красиво, не знаю как"** | `@plan` (интервью) | 🟡 Средне | 🟢 Высокая | 🟢 Полный |

---

## ✅ Итоговая Рекомендация

**Для дизайн-рефакторинга используй:**

```bash
# 1. Инициация
@plan Хочу отрефакторить [что именно] потому что [причина]

# 2. Интервью (отвечай подробно!)
[Общение с Prometheus]

# 3. Финализация
"Make it a plan"

# 4. Исполнение
/start-work

# 5. (Опционально) Продолжение после перерыва
/start-work  # автоматически подхватит с того же места
```

**Этот подход гарантирует:**
- ✅ Ничего не сломается (план проверен)
- ✅ Все шаги задокументированы
- ✅ Можно прервать и продолжить
- ✅ Atlas делегирует работу специалистам (frontend-ui-ux для UI)
- ✅ Автоматическая верификация каждого шага
